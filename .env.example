# Copy this file's variable names into Vercel's Environment Variables screen
# (Project Settings > Environment Variables), NOT into this file itself.
# Never commit real values here — this file is just a reference.

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-or-publishable-key
